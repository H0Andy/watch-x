# Electron binary mirrors (used via env in scripts; kept here for documentation)
# Windows/macOS/Linux: set ELECTRON_MIRROR if GitHub downloads are slow.
# Example PowerShell:
#   $env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
