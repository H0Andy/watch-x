{
  "name": "watch-x",
  "productName": "Watch X",
  "desktopName": "WatchX",
  "version": "1.0.0",
  "description": "Cross-platform CPU, GPU, memory, temperature and fan monitor",
  "author": {
    "name": "zheng",
    "email": "zheng@users.noreply.github.com"
  },
  "license": "MIT",
  "private": true,
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "icons": "node scripts/make-icons.cjs",
    "fetch:vendor": "node scripts/fetch-vendor.cjs",
    "build:spd-helper": "dotnet publish -c Release helpers/spd-helper/SpdHelper.csproj -r win-x64 --self-contained true -o helpers/spd-helper/dist",
    "test:spd": "dotnet test helpers/spd-helper/tests/SpdHelper.Tests.csproj --nologo && node --experimental-strip-types scripts/test-ddr5-spd-decode.mts && node --experimental-strip-types scripts/test-ddr5-spd-profiles.mts && npx tsx scripts/test-dram-db.mts",
    "dist": "npm run icons && npm run fetch:vendor && npm run build:spd-helper && vite build && electron-builder --publish never",
    "dist:win": "npm run icons && npm run fetch:vendor && npm run build:spd-helper && vite build && electron-builder --win --publish never",
    "dist:mac": "npm run icons && vite build && electron-builder --mac --publish never",
    "dist:linux": "npm run icons && vite build && electron-builder --linux --publish never",
    "pack:dir": "npm run icons && npm run fetch:vendor && npm run build:spd-helper && vite build && electron-builder --dir --publish never"
  },
  "dependencies": {
    "systeminformation": "^5.27.10"
  },
  "optionalDependencies": {
    "macos-temperature-sensor": "^2.1.0",
    "osx-temperature-sensor": "^1.0.8"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.3",
    "electron": "^37.4.0",
    "electron-builder": "^26.0.12",
    "png2icons": "^2.0.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vite-plugin-electron": "^0.29.0",
    "vite-plugin-electron-renderer": "^0.14.6"
  },
  "allowScripts": {
    "electron@37.10.3": true,
    "esbuild@0.28.2": true,
    "electron-winstaller@5.4.0": true
  }
}
